kafka:
  brokers: "9.134.132.205:19092" # 若远程连接替换为服务器IP
  topic: "test-topic" # 默认Topic名称

  # SASL认证（与docker-compose.yml中的配置完全一致）
  sasl:
    username: "admin"
    password: "123456"
    mechanism: "SCRAM-SHA-256"

redis:
  addr: "localhost:6379"
  password:
  db: 0

mysql:
  host: "localhost"
  port: 3306
  user: "root"
  password: "123@jiaru"
